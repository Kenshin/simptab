
define([ "jquery", "lodash", "i18n", "vo", "date", "error" ], function( $, _, i18n, vo, date, SimpError ) {

    "use strict";

    return {
        Render: function () {
            //TO-DO
        }
    };
});